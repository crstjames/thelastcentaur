/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 15 doesn't support these options
  // swcMinify: true,
  // serverOptions: {
  //   port: 3001,
  // },
};

module.exports = nextConfig;
